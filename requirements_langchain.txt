# Optional: LangChain + LangGraph prototypes (not required for core repo).
# Keep LangChain packages in the same minor family to avoid interface mismatches.
langchain>=0.2.10,<0.3.0
langchain-core>=0.2.10,<0.3.0
langchain-community>=0.2.10,<0.3.0
langgraph>=0.2.30,<0.3.0
